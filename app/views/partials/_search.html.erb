<%= search_form_for @q do |f|  %>
  <div class="input-group">
    <%= f.search_field :title_or_category_name_or_description_cont, placeholder: "Search by item name or category", class: 'form-control' %>
  <%= f.submit "Search", class: "btn btn-primary"%>
<% end %>
</div>